# Google Sheets 설정
google_sheets:
  spreadsheet_id: REDACTED_SPREADSHEET_ID
  service_account_path: REDACTED_SERVICE_ACCOUNT_PATH  # 환경변수 SERVICE_ACCOUNT_PATH로도 설정 가능

# 로깅 설정
logging:
  level: INFO

# 처리 설정
batch_size: 10
empty_row_threshold: 100
stock_type_cache_file: stock_type_cache.json 